{
  "name": "frogcode-im-sidecar",
  "version": "1.0.0",
  "private": true,
  "description": "IM bridge sidecar for Frogcode (Feishu + Claude Code)",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --target=node18 --format=cjs --outfile=../../binaries/frogcode-im-sidecar.cjs --minify --external:fsevents"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.58.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0",
    "@types/node": "^22.0.0"
  }
}
